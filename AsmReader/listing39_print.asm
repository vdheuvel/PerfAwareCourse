bits 16

mov si, bx
mov dh, al
mov cl, 12
mov ch, 244
mov cx, 12
mov cx, 65524
mov dx, 3948
mov dx, 61588
mov al, [BX + SI]
mov bx, [BP + DI]
mov dx, [BP]
mov ah, [BX + SI + 4]
mov al, [BX + SI + 4999]
mov [BX + DI], cx
mov [BP + SI], cl
mov [BP], ch